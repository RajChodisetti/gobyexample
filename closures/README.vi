$ go run closures.go
1
2
3
1


This will be the result

When we call nextInt() in print statement, its calling the anonymous function that was returned when we first initiated the nextInt variable and assigning to the return value of function intSeq()

When we intiate the intSeq again at newInts, the variable i got reinitiated.


